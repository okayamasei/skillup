<%= render 'layouts/userheader' %>
<div class="container">
<h3>マイページ</h3>

<span>登録情報</span>
<%= link_to "編集する", edit_end_user_path(@user),class:"btn btn-info" %>
<%= link_to "パスワードを変更する",class:"btn btn-info" %><br>

<table class="table">
    <tr>
        <th>氏名</th>
        <td><%= @user.first_name %> <%= @user.last_name %></td>
    </tr>
    <tr>
        <th>カナ</th>
        <td><%= @user.first_name_kana %> <%= @user.last_name_kana %></td>
    </tr>
    <tr>
        <th>郵便番号</th>
        <td><%= @user.now_postal_code %></td>
    </tr>
    <tr>
        <th>住所</th>
        <td><%= @user.now_address %></td>
    </tr>
    <tr>
        <th>電話番号</th>
        <td><%= @user.phone_number %></td>
    </tr>
    <tr>
        <th>メールアドレス</th>
        <td><%= @user.email %></td>
    </tr>
</table>


<span>配送先</span>
<p class="btn btn-info">一覧を見る</p><br>

<span>注文履歴</span>
<p class="btn btn-info">一覧を見る</p><br>
</div>
